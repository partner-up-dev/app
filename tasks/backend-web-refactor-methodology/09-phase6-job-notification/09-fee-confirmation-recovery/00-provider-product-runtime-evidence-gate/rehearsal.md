# `6-4a` Mental Rehearsal

- Official docs make allowances optional: omit them; do not encode an invented
  zero/default or owner.
- Official docs omit feeConfirm idempotency: record the fact; Sir's explicit
  risk acceptance means it does not create an `UNKNOWN` model or block work.
- Historic rows are unknown: leave them untouched rather than inventorying,
  classifying, or replaying them.
- Deployment evidence can validate rollout later; it is not a reason to block
  local source work.
