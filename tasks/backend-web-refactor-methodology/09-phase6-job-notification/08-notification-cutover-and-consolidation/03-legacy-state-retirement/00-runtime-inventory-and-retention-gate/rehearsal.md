# `6-3.3a` Mental Rehearsal

- Inventory finds a pending legacy PR-message Job: the inbox/API slice remains
  blocked unless its decoder is retained or a mapping is proved.
- Inventory finds only terminal rows: retention/archive still needs a decision;
  terminal does not mean disposable.
- API logs lack a build identifier: use the conservative support window rather
  than treating lack of attribution as zero old clients.
- A running worker cannot be paused: keep legacy registration and defer schema
  drop; do not create a mixed old/new deployment race.
