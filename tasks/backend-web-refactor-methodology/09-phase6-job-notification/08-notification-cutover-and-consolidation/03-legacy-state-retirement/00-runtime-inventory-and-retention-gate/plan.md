# `6-3.3a` Plan

1. Record the local static topology and all limits of that evidence.
2. Obtain the four requested shape-only evidence groups.
3. Classify each legacy Job family as retained decoder, verified migration or
   controlled drain; reject an unknown or `RUNNING` row from a destructive path.
4. Record archive and old-client sunset decisions, then mark this gate Go or
   No-Go. No application/schema edits occur in this child.

## Cheapest Credible Verification

One redacted operator inventory artifact plus a source reference ledger is
enough to decide whether the later slices may begin. A source `rg` result is
never substituted for production row/deployment/client evidence.
