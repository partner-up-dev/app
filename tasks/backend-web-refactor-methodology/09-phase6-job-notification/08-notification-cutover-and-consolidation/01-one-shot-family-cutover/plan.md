# `6-3.1` Execution Plan

1. Execute the child order in `../slice-map.md`. `01-activity-start-recovery`
   is a single recoverable vertical, not authorization to migrate every
   reminder.
2. Move confirmation through its own two-trigger proof.
3. Build and prove the named transaction-bound bridge by closing the
   WAITLIST_PROMOTED handoff debt before attaching it to new-partner, PR-ready
   or meeting-point changes.
4. Migrate each atomic family in its own child packet and preserve its exact
   source causation/recipient fact.
5. Make waitlist-alternative recovery a pure current-state query with no
   Notification -> PR mutation edge.
6. For each family, change domain and WeChat controller schedule/cancel/rebuild
   callsites, stop new opportunity writes, retain its old Job definition, and
   run the focused matrix before continuing.
7. Consolidate common rendering/revalidation only after all family-specific
   behavior is green.

Stop if an atomic family can commit without its Job, a recoverable family lacks
a deterministic reconstruction query, or a provider ambiguity is made
retryable without evidence.
