# `6-3.1g` Decision Log

| ID | Decision | Exit proof |
| --- | --- | --- |
| `6-3.1g-D1` | Waitlist-alternative is a recoverable current-state pair, not an atomic availability event. The generic policy preserves legacy active-only dedupe with a private REPLACE_ACTIVE key; it does not claim terminal-persistent ONCE_PER_CAUSE without a durable availability generation. | Repeated reconcile coalesces one active task; a later explicit current reconcile may create again after terminal state. |
| `6-3.1g-D2` | The source identity is `sourcePartnerId + sourceWaitlistCycleId + candidatePrId`, scoped by recipient. `waitlistCycleId` is mandatory in payload, causation, private key and query revalidation. | Reused Partner-row scenario makes an old-cycle task skip and permits a new-cycle task. |
| `6-3.1g-D3` | Eligibility is a curated PR-owned pure query shared by generic and legacy dispatch. It may read source/candidate/participant facts and apply pure join-boundary checks, but never imports temporal refresh or a PR command. | Spy/static dependency proof and unchanged PR status/slots after query/dispatch. |
| `6-3.1g-D4` | A stale raw-OPEN candidate after its relevant join boundary is conservatively skipped, rather than refreshed. For ordinary PRs the start boundary vetoes; for participation-policy PRs the pure join-lock boundary vetoes. | Fixed-time query unit covers stale OPEN and pre-boundary current candidate without persistence writes. |
| `6-3.1g-D5` | The business template remains distinct while privately reusing the existing WAITLIST_PROMOTED WeChat configuration and provider field mapping. | Generic owner/channel unit renders alternative wording and routes through the existing provider binding without leaking provider IDs. |
| `6-3.1g-D6` | Legacy alternative code becomes pending-row drain only: its handler/decoder/Delivery accounting and recipient-prefix cancellation remain, but source creation and controller rebuild use the generic reconciler. | Static source inventory finds no new concrete scheduler caller; `legacy_waitlist_alternative_pending_row_executes_through_drain_handler` executes a historical row through the retained handler. |
