# `6-5.1a` Mental Rehearsal

- Both source paths call the same global runner but use different budgets:
  report the difference; do not call them equivalent without a test seam.
- A test harness disables request-tail: a green full suite cannot prove it.
- SLS stdout exists: do not mistake it for searchable, retained operational
  evidence.
- Legacy delivery writes have no production reader: table retirement still needs
  retention, decoder-drain and O11y replacement proof.
