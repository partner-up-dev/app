# `6-5.1a` Plan

1. Inventory request-tail, external tick, Job attempt observer, delivery writers
   and FC trigger configuration.
2. Locate existing test-harness disables and prove which runtime paths lack a
   focused local test.
3. Separate bounded source contracts from deployment-only cadence/SLS/retention
   and alert evidence.
4. Name the narrow generic source seam that is independent of `6-4`.

## Cheapest Credible Verification

A source ledger plus focused existing JobRunner test inventory is sufficient.
It is not a substitute for FC/SLS staging observation.
