# 4-4.3 Verification And Promotion

Focused tests must assert persistence before claim and the payload boundary. Add a waitlist preference regression
test. Durable PR contract promotion waits for a named Browser-to-Backend journey.

## Result

Complete. A focused command test verifies waitlist persistence (including opt-in) precedes auth-response claim and
receives the original `Response`. The named set/no-create-replay rule is promoted with the 4-4.4 limitation recorded.
