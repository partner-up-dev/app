# 4-3.4 Execution Plan

1. Correlate the declared CI environment variables, durable deployment documents, and public redirect/header
   observations without resolving name disagreements by assumption.
2. When access is available, capture the WeChat console evidence and a state-free staging redirect chain.
3. Compare observed callback host/proto with Backend callback construction and frontend configured origin.
4. Inventory direct callback consumers before proposing their retirement or a navigation-only protocol.
5. Record a go/no-go decision for a future topology change. Keep a missing fact as a blocker rather than replacing
   it with a source-derived claim.
