# 4-3 Durable Documentation Plan

## Promotion Result

The durable owner, `docs/30-unit-tdd/wechat-oauth-handoff.md`, now states the compact generative rule proven by
4-3: a received terminal handoff result closes its one-shot nonce and leads to fresh login or visitor continuation;
transport uncertainty retains the nonce for retry. The promotion also records explicit no-token behavior for
expected OAuth failures and the direct JSON compatibility boundary.

## Non-Promotion

Do not promote precise production URLs, provider-console configuration, FC edge forwarding behavior, cookie
observation, legacy-consumer retirement, or System harness limitations as durable architecture. Keep those as
task-local evidence until independently observed.

The shared session contract remains unchanged unless 4-3 discovers an actual contradiction in the active
persisted-public-user rule. The PRD remains unchanged because visitor continuation and authentication eligibility
do not change product intent.
