# 4-5 Execution Plan

1. Reconfirm the exact route-policy inventory and verify the hook has no production mount.
2. Refine the existing decision into one router-entry guard and register it once in app bootstrap before
   `app.use(router)`, without changing page or domain code.
3. Add the smallest focused proof of anonymous eligible-route redirect ordering, navigation abortion, one-time
   app-bootstrap registration, and a delayed-bootstrap superseded-navigation branch before accepting the guard.
4. Re-run facade consumer inventory across source, scripts, CI, FC entrypoints, and public package exports; delete
   only if all local evidence is zero.
5. Run focused tests, full Web unit proof, Backend type/build if facade files are deleted, and root static gates in
   proportion to changed surfaces.
6. Promote the product promise to PRD and the reusable ownership rule to Web/OAuth durable docs. Record the known
   System-harness limitation and all remaining external evidence branches.

The initial implementation omitted the delayed-bootstrap superseded-navigation proof. The completion review found
that omission was material; this plan's source work is not re-opened implicitly, but the candidate repair packet owns
the missing branch before Phase close.
