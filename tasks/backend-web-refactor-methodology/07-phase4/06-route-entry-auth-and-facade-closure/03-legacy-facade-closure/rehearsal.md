# 4-5.3 Rehearsal

If no local import or runtime entry reaches either file, bundling has no public entry to expose it and type/build
remain sufficient local proof. If an external consumer cannot be inventoried, retain rather than infer deletion.
