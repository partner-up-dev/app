# 4-0 Decision And Risk Register

| ID | Status | Evidence-backed statement | Required decision / stop condition | Proposed owner |
| --- | --- | --- | --- | --- |
| P4-R1 | Contained locally + rollout observation pending | Public preflight proved arbitrary-origin credentialed CORS reflection in both environments. `4-1` now confines CORS/return-target to the configured same-environment pair; the full OAuth exposure chain still depends on callback/cookie/browser facts. | Provider topology remains a `4-3` prerequisite; a normal-network runner must make the state-free staging header observation. | `4-1`, then `4-3`. |
| P4-R2 | Resolved for public context | Every subject-bound public bearer now resolves through the User active-public-identity query before controller use; status/role changes drive canonical public identity or anonymous downgrade. Focused backend and Browser-to-Backend proof agree. | Admin/operator bearer revalidation remains a separate context and is not claimed resolved. | `4-2` complete; later admin-session owner. |
| P4-R3 | Compatibility decision | Navigation handoff and direct callback page expose two callback shapes. | Retain, migrate or retire the direct callback only after consumers and provider topology are proven. | `4-3` callback compatibility. |
| P4-R4 | Product/behavior decision | Pending command replay clears a stored action before its handler; create deliberately has no replay. | Choose retry versus at-most-once semantics for replayable commands; preserve no-create-replay. | `4-4` escalation protocol. |
| P4-R5 | Resolved for public context | Public identity is only `anonymous` or `authenticated`; classifier, issuers, public middleware and browser projection reject `service`/`analytics`, including mixed rows. | Keep admin client/middleware context separate; do not treat this as operator bearer validation. | `4-2` complete. |
| P4-R6 | Scope decision | Route auto-login is tested but has no discovered production invocation. | Confirm whether it is a dormant feature to wire or a compatibility path to retire; do not infer product intent. | Sir, then `4-5`. |
| P4-R7 | Deferred structural finding | Legacy WeChat session/login facades appear unused; the Auth runtime SCC has five value edges. | Do not delete or mechanically move them until replacement behavior is proven in its owning slice. | `4-4` / `4-5`. |
| P4-R8 | New 4-3 input | Navigation OAuth callback can create a handoff before its public issuer rejects an operator-bearing row, so the later handoff presently reaches a global `500` path without issuing a public operator token. | Choose and prove callback/handoff failure semantics with mock-provider topology evidence; do not alter it incidentally. | `4-3`. |
