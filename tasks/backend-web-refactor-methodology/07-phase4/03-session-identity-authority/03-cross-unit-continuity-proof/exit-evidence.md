# 4-2.3 Exit Evidence

The scenario starts at `/` in a clean browser, observes the initial anonymous UUID/token, removes only the token and
reloads to prove the same UUID is restored, then disables that persisted row and repeats the reload to prove a
different anonymous UUID/token appears. The database action establishes only the hidden disabled state; continuity
assertions are observed through browser storage after real application bootstrap.

This agrees with the focused backend and Web proofs, so it unlocks the compact cross-unit contract promotion.
