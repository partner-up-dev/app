# `8-7.1` Implementation Rehearsal

Re-run the same scoped graph/report commands used at `8-0`, then compare
semantic categories rather than raw file/edge counts. Sample every remaining
finding and every retained compatibility row. A zero-count search is recorded
with its scope; it is not generalized beyond that scope.
