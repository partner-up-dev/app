# `8-7.3` Remaining Handoff

## External Evidence

- Phase 4 rollout CORS/header observation, provider-console/edge callback
  authority and optional faithful canonical-host browser proof.
- Phase 5 `5-7a` provider/runtime proof using operator network/edge capture,
  provider-signed request and captured Backend receipt. It must not restore
  retired pseudo-observability.

## Independent Future Work

- professional program observability and the replacement/retention proof
  required before retiring `notification_deliveries`;
- Node runtime/pnpm, Oxc migration and quality-gate orchestration task packets;
- Drizzle journal/generator provenance.

## Product / Evidence-gated Deferrals

- Rental payment/schema/data-retention choices;
- final-fare adjustment/refund policy;
- Admin read composition and Viewer Bill list query optimization;
- any performance cleanup without request-count/query-plan evidence.

## Retained Compatibility

- terminal WeChat OAuth callback RPC;
- legacy CaoCao callback route until provider/deployment exit proof;
- inert historical `notification_deliveries` rows;
- registered Backend package-root legacy surface.

## Local Git State

Phase 8 is a working-tree local completion on branch `develop`; `HEAD` remains
`cf6cd736` and no Phase 8 commit has been created.

- staged diff: empty;
- `package.json`, `pnpm-lock.yaml` and `pnpm-workspace.yaml`: unstaged modified,
  independently owned;
- `tasks/project-node-runtime/`, `tasks/oxc-toolchain-migration/` and
  `tasks/quality-gate-orchestration/`: untracked independent packets;
- `.dev-server/`: untracked System-scenario runtime output containing three
  generated poster images, excluded from Phase 8.

This final-state evidence proves separation and staging status only. It does
not claim byte history for protected paths because no entry hash was recorded.
