# `7-2` Implementation Rehearsal

## Batch 1 — Deployment Coupling

1. Remove `s.yaml` SLS variables/config.
2. Remove workflow and validator inputs in the same batch.
3. Update Backend runtime/observability truth.
4. Run the narrow configuration and packaging/deploy-template validators.

Do not proceed if the deployment tool requires an uncharacterized replacement.

## Batch 2 — Structured And Debug Protocols

1. Remove callback-router log-event types/default output/injected log contract.
2. Rebase tests on route decision, upstream selection, status and duration
   behavior that remains product/runtime behavior.
3. Remove Commerce debug graph in one ledger-driven pass.
4. Run focused Backend/Web unit and Commerce scenarios.

## Batch 3 — Remaining Production Diagnostics

1. Apply the ratified runtime-versus-CLI classification.
2. Remove sensitive OAuth/WeCom/request/runtime diagnostics in coherent owner
   batches.
3. Do not replace output with another console wrapper.
4. Run owner-focused tests after each meaningful batch.

## Batch 4 — Persisted/External Surfaces

1. Forward-delete `operation_logs`, its entity/repository/service, all
   production writers/tests and stale guidance. Historical rows are
   intentionally discarded by the ratified migration.
2. Inventory/delete authorized external saved SLS artifacts when evidence says
   they exist, or close the boundary from explicit operator evidence/decision.
3. Keep local proof, operator evidence and platform actions separately
   labelled.

Final disposition: Sir confirms no saved SLS queries/dashboard are configured
and closes further platform-artifact inventory; no platform access or deletion
by Codex is claimed.

## Final Proof

- zero-reference and import audits;
- no new logger/signal/exporter dependency;
- deployment configuration proof;
- full Backend/Web unit and relevant scenarios;
- durable docs and worktree scope audit.
