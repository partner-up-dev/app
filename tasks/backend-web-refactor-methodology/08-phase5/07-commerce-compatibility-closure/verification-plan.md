# 5-6 Verification Plan

- deterministic import graph and `rg` zero-consumer proof;
- focused tests for every migrated owner surface;
- backend/web typecheck and relevant build reachability;
- one affected Commerce system scenario per user-visible path before deleting a route/facade.
