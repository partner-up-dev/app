# 5-7 Verification Plan

- staging signed callback smoke with a test provider/order identity;
- CaoCao edge body/header/token negative checks;
- payment notify URL and certificate/origin configuration observation;
- final Phase review uses source, focused tests, backend/system scenarios, and external evidence as separate proof
  classes rather than flattening them into one green claim.
