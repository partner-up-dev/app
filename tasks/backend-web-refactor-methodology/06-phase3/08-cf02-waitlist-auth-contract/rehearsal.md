# Slice 3-8 Mental Rehearsal

| Fork / surprise | Decision rule | Cheapest proof |
| --- | --- | --- |
| Only documentation is stale | Make a documentation-only correction; do not churn runtime | typed contract/source trace |
| Runtime returns credentials in body | Stop and decide whether it is a security defect or versioned contract | response capture + Backend scenario |
| Waitlist success relies on notification side effects | Preserve current side effect; defer delivery architecture to Job/Notification | focused journey and side-effect assertion |
| Required change expands into global auth | Stop and route to User/Auth phase | focused diff/import graph |

Phase 3 exits only when both conflict rows are resolved, all eight slice logs are current, fitness reports zero new
violations and the required focused/full System gates are green.
