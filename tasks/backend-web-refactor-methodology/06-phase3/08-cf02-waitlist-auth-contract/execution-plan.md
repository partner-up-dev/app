# Slice 3-8 Execution Plan

1. Trace waitlist Browser → HTTP → Backend → session-rotation behavior and identify the authoritative contract.
2. Correct or explicitly version the durable wording before behavior mutation.
3. Add/refresh Backend waitlist characterization and Web typed-response handling tests.
4. Implement only the mismatch proven by evidence; keep header transport as the sole channel unless intent changes.
5. Run focused Backend/Web tests, waitlist scenarios, targeted/full System and architecture fitness.
6. Reconcile the Phase 3 packet, risks, durable promotion log and exit evidence.
