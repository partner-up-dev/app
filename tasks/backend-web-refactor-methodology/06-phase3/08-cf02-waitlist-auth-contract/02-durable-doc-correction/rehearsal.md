# 08B Mental Rehearsal

| Fork | Decision | Cheapest check |
| --- | --- | --- |
| One stale lifecycle sentence only | Correct only that sentence and link to shared transport truth | focused `rg` + diff |
| Another durable document claims body credentials | Add it only if it shares the same authority; otherwise record an alignment fork | authority routing + source trace |
| Runtime/body/header facts are ambiguous | Do not prose-patch around ambiguity | return to 08A/08C evidence |
| Wording needs new auth protocol semantics | Stop; that belongs to User/Auth | compare against shared RPC transport |

The desired diff is documentation-only and must not create an invented response DTO or a waitlist-specific exception.
