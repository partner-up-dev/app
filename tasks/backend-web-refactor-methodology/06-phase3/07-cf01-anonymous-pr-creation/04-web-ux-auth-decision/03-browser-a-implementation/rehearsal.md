# Rehearsal

Anonymous submit awaits bootstrap, observes the authenticated predicate, opens disclosure and makes zero create
POSTs. Cancel closes it without changing fields. Confirm is the only path to `requestWeChatOAuthLogin(current URL)`;
there is no payload retention or OAuth-return replay. Authenticated submit makes one existing create POST.
