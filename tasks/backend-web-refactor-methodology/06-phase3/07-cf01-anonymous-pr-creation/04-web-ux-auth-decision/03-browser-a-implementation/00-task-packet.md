# 07D Browser A implementation packet

Owner: Browser A Web bounded implementation.

Scope: shared pre-create auth gate/disclosure, four Web command owners, removal of the structured server-draft
affordance, and removal of only the `PR_DISCOVERY_CREATE` pending action. Backend/OAuth protocol/global auth policy,
unrelated pending actions, and durable docs are out of scope.
