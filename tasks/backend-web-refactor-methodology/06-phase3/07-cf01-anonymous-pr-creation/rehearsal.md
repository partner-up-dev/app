# Slice 3-7 Mental Rehearsal

| Fork / surprise | Decision rule | Cheapest proof |
| --- | --- | --- |
| Anonymous means local draft, server draft or auth-first form access | Decide visible product continuity and authority before storage mechanism | PRD outcome table + one browser journey |
| OAuth return loses entered data | Use the existing explicit pending-action protocol only if it satisfies the chosen intent | reload/OAuth replay characterization |
| Backend permits a path Web never exposes, or vice versa | Treat as contract conflict, not frontend copy drift | typed request + Backend scenario + System |
| Fix expands into global auth redesign | Stop and route that work to User/Auth phase | focused diff and import graph |

Rollback is the pre-slice create/auth behavior plus retained characterization tests; durable intent is reverted only
if the product decision itself is withdrawn.
